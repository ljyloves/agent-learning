# 企业级安全 Agent 工作流平台项目进度计划

## 1. 项目定位

项目名称建议：

- 企业级安全 Agent 工作流平台
- Enterprise Secure Agent Workflow Platform

项目目标：

构建一个面向企业知识任务的多智能体工作流平台，支持文档知识库、RAG 检索、多 Agent 协作、工具调用、权限控制、人工审批、执行链路追踪和基础安全治理。

这个项目不只是一个普通知识库问答系统，而是一个可以处理复杂任务、展示执行过程、控制工具风险、并且能在面试中讲清楚工程设计的 Agent 应用系统。

## 2. 推荐技术栈

后端：

- Python
- FastAPI
- LangGraph（含 checkpoint 持久化）
- LangChain（文档加载、文本切分）
- PostgreSQL
- Qdrant（向量存储与混合检索）
- Redis
- Docker Compose
- SSE（Server-Sent Events，全链路流式推送）
- FlagEmbedding / BAAI 系列 Embedding 模型（bge-large-zh-v1.5）
- Cross-Encoder Reranker（bge-reranker-v2-m3）
- BM25（rank_bm25 或 Elasticsearch）
- structlog（结构化日志）
- Alembic（数据库迁移）

前端：

- Next.js
- React
- TypeScript
- Tailwind CSS 或其他成熟 UI 方案
- EventSource / fetch SSE 客户端（流式接收 Agent 执行进度）

核心能力：

- 文档上传与解析（PDF / Word / TXT / Markdown）
- 混合检索：BM25 关键词 + 向量语义 + Reranker 精排
- RAG 问答 + 引用来源高亮
- 层级索引：摘要索引 → 详细 chunk 两级检索
- Planner / Retriever / Executor / Verifier 多 Agent 工作流
- 全链路 SSE 流式推送（用户可实时看到每步执行状态）
- 会话记忆与跨任务上下文管理（LangGraph checkpoint）
- 工具注册与权限控制（含参数级白名单校验）
- 高风险操作人工审批（Human-in-the-loop）
- Trace 日志与成本统计
- 系统指令与用户数据结构化隔离
- Prompt Injection 与敏感信息检测
- 检索质量自动评估（MRR、NDCG@10、Recall@K）

## 3. 21 天完整开发计划

### 第 1-3 天：项目搭建与基础架构

目标：

把项目骨架搭起来，保证前后端、数据库和基础服务可以正常运行。

要完成：

- 搭建 FastAPI 后端项目
- 搭建 Next.js 前端项目
- 配置 PostgreSQL
- 配置 Qdrant 向量数据库或预留 collection 结构
- 配置 Redis
- 编写基础 Docker Compose
- 建立基础目录结构
- 实现基础页面：首页、知识库列表页、任务列表页、后台管理入口

产出：

- 项目可以本地启动
- 前后端可以连通
- 数据库可以正常读写
- README 中包含启动方式

### 第 4-6 天：知识库与 RAG（增强版）

目标：

先做出企业文档问答能力，形成项目的第一条主链路。这一阶段不只是"能跑"，而是**检索质量要经得起追问**。

要完成：

**文档处理链路：**
- 文档上传（拖拽 + 进度条）
- 文档解析，支持 PDF / Word / TXT / Markdown 四类
- 表格内容提取与结构化存储
- 文本切分（语义分块 + 滑动窗口重叠，chunk_size 512–1024）
- 层级索引：先生成文档级摘要索引，再检索详细 chunk

**检索链路（混合检索）：**
- Embedding 生成（bge-large-zh-v1.5，1024d）
- 向量入库（Qdrant collection，HNSW 索引）
- BM25 关键词检索（rank_bm25 内存版即可）
- 混合检索：BM25 召回 top-20 + 向量召回 top-20 → 去重融合
- Cross-Encoder Reranker 精排（bge-reranker-v2-m3）
- 最终返回 top-5 chunk

**前端：**
- 知识库上传页面（含解析进度）
- 知识库问答页面（对话式）
- 回答中引用来源高亮 + 点击跳转原文
- 检索结果置信度可视化（相似度分数展示）

**检索质量评估（提前做）：**
- 准备 20 条测试 query + 预期命中文档标注
- 实现 MRR、NDCG@10、Recall@K 自动计算脚本
- 每次调检索策略后跑一轮，确保不退化

产出：

- 用户上传文档后可以基于文档提问
- 回答能显示命中文档 / chunk 来源 + 相似度分数
- 有混合检索 vs 纯向量检索的效果对比数据
- 可以演示"搜索'合同违约'时，BM25 精准命中条款 + 向量补充语义相关段落"

### 第 7-9 天：Agent Workflow 主流程（增强版）

目标：

从普通 RAG 升级为多步骤 Agent 工作流，核心差异化在于**流式推送 + 会话记忆**，让用户实时看到 Agent 的思考过程，而不是等 30 秒才出结果。

建议 Agent 架构：

- Planner Agent：拆解用户任务为子步骤
- Retriever Agent：混合检索相关知识（调用第 4-6 天的检索链路）
- Executor Agent：生成结果或决定调用工具
- Verifier Agent：检查答案是否有依据，拒绝无来源回答

要完成：

**LangGraph 核心：**
- 定义 TaskState（含 messages、retrieved_docs、tool_calls、approval_status）
- 设计工作流节点 + 条件边（Planner → Retriever → Executor → Verifier → [loop/end]）
- 支持多步骤执行与节点间状态流转
- 每一步保存输入、输出、状态、时间戳
- 支持失败重试（单节点重试 + 不丢已完成步骤的结果）
- 使用 LangGraph checkpoint 持久化任务状态

**全链路 SSE 流式推送（核心亮点）：**
- 每个 Agent 节点开始/结束时推送事件到前端
- 事件类型：thinking（思考中）、retrieving（检索中）、tool_calling（调用工具）、generating（生成中）、waiting_approval（等待审批）、done / error
- 前端用 EventSource 接收，展示实时执行进度（步骤条动画 + 中间结果预览）
- 流式输出 LLM 生成内容（逐 token 展示，不做完才显示）

**会话记忆与上下文管理：**
- 用户对话历史存储（Redis + PostgreSQL 双写）
- 同一会话内后续问题可引用前面的检索结果
- 跨任务上下文传递（上一个任务的结论可作为下一个任务的上下文）
- 对话窗口管理（超出 token 限制时自动摘要压缩历史）

**前端：**
- 任务执行进度（步骤条 + 每步状态图标）
- 流式展示 LLM 输出
- 中间步骤可折叠展开查看详情
- 检索到的 chunk 实时展示

产出：

- 用户输入复杂任务，系统可自动拆解并逐步执行
- 页面实时展示每步 Agent 的思考过程（不是一次性返回）
- 多轮对话能记住上下文
- 面试时可讲清楚 streaming 架构 + checkpoint 机制

### 第 10-12 天：工具调用与权限控制（增强版）

目标：

不只做 mock 工具，而是**集成真实 API + 参数级白名单校验**，让面试官看到你理解工具调用的真实复杂度。

工具设计（3 个 mock + 2 个真实集成）：

| 工具 | 类型 | 说明 |
|---|---|---|
| 文档检索工具 | 内部调用 | 封装第 4-6 天的混合检索链路 |
| 报告生成工具 | 内部调用 | 基于检索结果生成结构化报告（Markdown/PDF） |
| 网页搜索工具 | Mock | 返回预设结果，后续可接 SearXNG |
| GitHub Issues 查询 | **真实 API** | 查询指定 repo 的 open issues，展示 closed/fixed 统计 |
| Slack Webhook 通知 | **真实 API** | 高风险操作触发时发送通知到指定频道 |

权限控制（三层校验，不只是审批）：

**第一层：工具注册 + 风险分级**
- 工具注册表（名称、描述、参数 JSON Schema、风险等级）
- 风险等级：safe / sensitive / dangerous
- safe 工具自动放行，sensitive 需要确认，dangerous 必须审批

**第二层：参数级白名单校验**
- 每个工具的每个参数定义允许值范围（如 GitHub Issues 只能查指定仓库，不能传任意 org/repo）
- 参数类型强制校验（不允许 LLM 传非法参数类型）
- 参数长度/格式限制（正则白名单，如 repo 名只能 `[a-zA-Z0-9_-]+/[a-zA-Z0-9_-]+`）

**第三层：高风险操作审批**
- dangerous 工具调用自动挂起 → 创建审批单 → 通知用户
- 审批通过后继续执行，拒绝后跳过该步骤
- 审批记录永久留存

要完成：
- 工具注册表 API（CRUD + 启用/禁用）
- 统一工具调用接口（ToolExecutor，封装参数校验 + 权限检查 + 日志）
- 工具调用日志（输入参数、返回结果、耗时、成功/失败）
- 前端工具管理页面（查看工具列表 + 开关 + 查看调用日志）
- 前端审批流程（审批列表 + 详情 + 通过/拒绝）

产出：
- Agent 能根据任务自动选择合适的工具
- GitHub Issues 查询返回真实数据（面试时调用一次，结果真实可见）
- 后台可配置工具启用/禁用 + 查看每类工具的调用统计
- 危险操作不会直接执行（参数级 + 审批级双重拦截）

### 第 13-15 天：人工审批与审计日志

目标：

体现企业级安全可控能力。

要完成：

- 审批表设计
- 审批状态：待审批 / 已通过 / 已拒绝
- 高风险步骤暂停执行
- 用户审批后继续任务
- 完整审计日志，包括任务 ID、Agent 名称、输入、输出、调用工具、工具参数、耗时、token 消耗、错误信息

前端页面：

- 审批列表
- 审批详情
- 任务 Trace 页面

产出：

- 可以演示“Agent 想执行敏感操作，但必须人工确认”
- 可以看到完整执行链路
- 面试时可以讲清楚 Human-in-the-loop 设计

### 第 16-17 天：安全与质量增强（增强版）

目标：

安全不是"加个正则匹配"，而是**从架构层面做隔离**，让面试官看到真正的安全工程思维。

要完成：

**Prompt Injection 防御（架构级）：**
- 系统指令与用户数据**结构化分离**：sys_prompt 和 user_content 在 API 层面独立字段传递，不做字符串拼接
- 用户输入进入 LLM 前标记为 `[USER_INPUT_START]...[USER_INPUT_END]`
- 检测用户输入中是否包含"忽略之前的指令"、"你现在是"等常见注入模式
- 注入攻击记录到安全日志（时间、用户、payload、模型响应）

**敏感信息检测：**
- 正则 + 规则引擎检测：手机号、身份证号、邮箱、API Key、Token、密码、内网 IP
- 检测命中后：屏蔽内容不传给 LLM，或标记 `[REDACTED]`
- 前端展示时二次脱敏（后端返回已脱敏，前端不展示原始值）

**检索与生成质量控制：**
- 检索结果置信度阈值：相似度 < 0.6 的 chunk 不进入生成上下文
- Verifier 检查回答中每个断言是否有对应来源引用
- 无依据回答 → 拒答："根据现有资料，我无法确认该信息，建议补充相关文档"
- 低置信度回答 → 标记 + 前端展示"此回答置信度较低"警告

**因果关联验证（进阶）：**
- 用 NLI（自然语言推理）模型验证：检索到的 chunk 是否真的支撑生成结论
- 识别"看起来相关但其实无关"的检索噪音
- 对 Verifier 判定为"无来源支撑"的回答，记录为 quality_issue 事件

产出：
- 不是简单 Demo，而是有分层安全架构的真实系统
- 可以回答追问："为什么不用字符串拼接做系统提示词？""NLI 模型怎么做因果验证？"
- 面试时可展示注入攻击被拦截的实际日志

### 第 18-19 天：评测与可观测性（增强版）

目标：

用数据而非感觉说话，让项目从"看起来不错"升级为"有量化指标支撑"。

要完成：

**离线评测体系：**
- 准备 30–50 条测试问题（覆盖简单事实查询、多步推理、跨文档综合、边界无答案）
- 评测指标：
  - 检索：MRR、NDCG@10、Recall@5、Precision@5
  - 生成：答案相关性（LLM-as-judge 打分 1–5）、是否引用来源（是/否）、幻觉率
  - 系统：平均响应时间、P50/P95/P99 延迟、平均 token 成本、工具调用成功率
- 每次改检索策略/提示词后跑一轮评测，输出对比报告
- `make eval` 一键执行

**实时 Dashboard：**
- 总任务数、成功率、失败率（今日 / 本周 / 总计）
- 平均耗时、P95 延迟趋势图
- Token 消耗统计（按模型、按任务类型）
- 工具调用次数 Top-N + 成功率
- 失败原因分布（检索无结果 / LLM 超时 / 工具失败 / 审批拒绝）
- 审批队列长度 + 平均等待时间

**结构化日志（给面试官看的细节）：**
- 用 structlog 替代 print，每条日志带 trace_id、agent_name、task_id
- JSON 格式输出，`docker logs` 直接喂给 jq 分析
- 关键节点日志：检索开始/结束、LLM 调用开始/结束（首 token 时间）、工具调用开始/结束

**告警规则（写出来即可，不一定要对接告警系统）：**
- LLM 失败率 > 5% 持续 5 分钟
- 审批队列积压 > 10 条
- P95 延迟 > 60s
- 检索命中率 < 50%（可能是向量库问题）

产出：
- 可以用评测报告证明检索升级后 MRR 提升了多少
- Dashboard 截图放进简历和 PPT
- 面试时说"每次上线前跑一轮 eval"比说"我感觉效果不错"强十倍

### 第 20-21 天：简历包装与演示打磨

目标：

把项目整理成可展示、可讲解、可写进简历的成果。

要完成：

- 完整 README（含架构图、技术栈、启动方式、Demo GIF）
- 系统架构图（Mermaid / Excalidraw，标注每个组件职责）
- Agent 工作流状态流转图
- Demo 截图和录屏（覆盖核心流程）
- 3 分钟演示流程（精心设计节奏）
- 简历项目描述（3 个版本：50字简版 / 100字标准版 / 200字详版）
- 面试讲解稿（含常见追问准备）
- 评测结果可视化图表

最终演示流程（8 步，全程展示流式推送）：

1. 上传一份合同或公司制度文档（展示文档解析 + 混合检索）
2. 输入复杂任务：”分析这份合同中的付款、违约、保密和终止条款风险，并生成审查报告”
3. **SSE 实时展示** Planner 拆解步骤（用户看到 Agent 在”思考”）
4. **SSE 实时展示** Retriever 混合检索过程（BM25 + 向量命中 chunk 逐条浮现）
5. **SSE 实时展示** Executor 流式生成报告（逐 token，不等全部完成）
6. **切换场景**：执行高风险操作（如”通知运维团队发版”）→ 触发审批流程
7. 审批详情页 → 人工点击”通过” → Agent 继续执行
8. Trace 页面展示完整链路 + 成本统计 + 各步骤耗时
9. Dashboard 展示总览数据（成功率、token 消耗趋势、检索质量趋势）

演示关键技巧：
- 演示时 Langfuse 开源 UI 展示调用链路比截图更有说服力
- 流式推送是视觉冲击力最强的部分，一定要确保不卡顿
- 审批流程展示”暂停 → 用户操作 → 继续”的完整来回

## 4. 14 天压缩版计划

如果时间更紧，可以按 14 天版本推进。

- 第 1-2 天：项目骨架、数据库、基础页面
- 第 3-5 天：文档上传、解析、RAG 问答
- 第 6-8 天：LangGraph Agent 工作流
- 第 9-10 天：工具调用与权限控制
- 第 11-12 天：人工审批与 Trace 日志
- 第 13 天：安全检测与简单评测
- 第 14 天：README、简历描述、Demo 打磨

## 5. 优先级建议

**P0 — 必须完成（决定项目下限）：**

- 混合检索（BM25 + 向量 + Reranker）
- Agent 工作流（LangGraph + 流式 SSE）
- 工具调用（含至少 1 个真实 API 集成）
- 全链路 Trace 日志（结构化 JSON 日志）
- README + 演示流程

**P1 — 强烈建议（决定项目上限）：**

- 会话记忆与上下文管理（LangGraph checkpoint）
- 参数级权限控制 + 人工审批
- 引用来源高亮 + 置信度展示
- 检索质量评测脚本（MRR/NDCG）
- 错误处理与降级（LLM 重试 + 向量库不可用降级）
- Alembic 数据库迁移

**P2 — 锦上添花（时间富余再做）：**

- NLI 因果关联验证
- 沙箱工具执行
- 结构化日志 + 告警规则（写出来就行）
- 多 Agent 辩论机制
- Langfuse 集成
- API Key 鉴权

**可以砍掉：**

- 复杂登录权限体系
- 多租户
- Kubernetes
- 真正邮件发送
- 复杂前端动画
- 多模型路由（先用单模型）
- 完整企业权限体系

## 6. 建议开发模式

整体架构和主要工程实现可以交给 Codex 来搭建，但你需要参与关键设计和核心模块理解。

推荐分工：

- Codex 负责项目骨架、后端架构、数据库模型、工作流节点、前端页面、README 和演示材料。
- 你重点理解模块边界、Agent 状态流转、数据表设计、工具权限、审批流程和 Trace 日志。
- 每完成一个模块，都要能讲清楚它为什么存在、怎么工作、如果出错怎么处理。
- 你可以亲自改一部分关键模块，例如新增一个 Tool、新增一个 Agent 节点、新增一条审批规则。

这样最终项目不是黑盒，而是一个你能讲清楚、也能继续修改的简历项目。

## 7. 简历描述建议

可以写成：

设计并实现企业级 Agent Workflow 平台，基于 Planner-Executor-Verifier 架构支持复杂任务拆解、RAG 知识检索、插件式工具调用、人工审批和执行链路追踪；引入权限白名单、敏感操作确认和 Prompt Injection 检测机制，提升企业知识任务自动化的安全性与可控性。

如果想强调工程能力，可以补充：

- 使用 FastAPI、LangGraph、PostgreSQL、Qdrant 和 Redis 构建后端核心链路。
- 实现文档解析、向量检索、引用来源展示和多步骤任务执行。
- 设计工具注册表、风险等级、参数校验和人工审批机制，降低 Agent 越权调用风险。
- 构建任务 Trace 页面，记录模型调用、工具调用、耗时、token 成本和失败原因。

## 8. 面试讲解亮点

面试时重点讲这几个点：

**1. 为什么不是普通 RAG —— 混合检索 + 多 Agent 协作**

普通 RAG 只能回答问题。这个项目用 BM25 + 向量 + Reranker 做混合检索，再通过 Planner-Retriever-Executor-Verifier 协作完成复杂任务。关键是用户可以**实时看到每步的思考过程**（SSE 流式推送），不是等 30 秒出黑盒结果。

**2. 为什么要做三层权限控制**

Agent 调用工具不只是"审批或不审批"。参数级白名单校验确保即便 LLM 产生幻觉参数，系统也会拦截——比如限制 GitHub Issues 查询只能访问指定仓库。这三层是：参数校验层 → 风险等级层 → 人工审批层。

**3. 为什么要做全链路 Trace**

Agent 系统的不确定性来自 LLM + 检索 + 工具三层。没有 Trace，出了问题不知道是检索没召回、LLM 理解错了、还是工具挂了。结构化 JSON 日志 + trace_id 关联每一步，`docker logs | jq` 就能定位。

**4. 为什么要有 Verifier + NLI 验证**

Verifier 不只是"Yes/No"判断。用 NLI 模型验证检索到的 chunk 是否真的支撑生成结论，识别"看起来相关但其实无关"的检索噪音。无依据时拒答——这在企业场景比礼貌性胡编重要得多。

**5. 流式架构的设计决策**

为什么用 SSE 而不是 WebSocket？单向推送场景 SSE 更轻、更容易在反向代理后工作、浏览器原生支持断线重连。每个 Agent 节点的状态变更都作为独立事件推送，前端不需要维护复杂状态机。

**6. 项目最大的差异化**

不是聊天机器人，而是一个**可控、可审计、可扩展**的企业 Agent 执行平台。能做到：流式展示思考 → 检索过程可视化 → 工具调用有安全边界 → 每步可追溯 → 效果有数据支撑。

## 9. 差异化垂直场景设计

不要做"泛知识库问答"，要选一个有真实业务痛点的**垂直场景**，让 Agent 架构和工具设计有说服力。

### 推荐场景 A：安全事件响应 Agent（首选）

| 维度 | 内容 |
|---|---|
| 输入 | 安全告警（如 WAF 拦截日志、异常登录通知） |
| Planner | 判断告警类型 → 拆解调查步骤 |
| Retriever | 查安全知识库（历史事件处理 SOP）+ 查资产信息（CMDB API） |
| Executor | 生成事件分析报告 + 调 Slack API 通知 oncall |
| Verifier | 检查报告结论是否有 SOP 依据 |
| 审批 | "通知全员"、"封禁 IP" 等敏感操作需审批 |
| 真实 API | Slack Webhook、GitHub Issues（跟踪事件处理进度） |

面试开场白："基于真实安全运维场景，设计了一个自动分析安全告警并生成处理建议的 Agent 系统。"

### 推荐场景 B：代码审查 Agent

| 维度 | 内容 |
|---|---|
| 输入 | GitHub PR diff |
| Planner | 判断改动范围 → 确定审查维度（安全/性能/规范） |
| Retriever | 查编码规范文档 + 历史类似 PR 的审查记录 |
| Executor | 生成审查意见 + 调 GitHub API 提交 PR review |
| Verifier | 检查每条意见是否有规范依据 |
| 审批 | "直接 merge"、"关闭 PR" 等操作需审批 |

### 推荐场景 C：数据 Pipeline 排障 Agent

| 维度 | 内容 |
|---|---|
| 输入 | 异常日志 / 数据质量告警 |
| Planner | 判断异常类型 → 确定排查路径 |
| Retriever | 查 Schema 文档 + 历史工单 + Runbook |
| Executor | 生成排查步骤 + 定位根因 + 建议修复方案 |
| Verifier | 检查根因判断是否有 Runbook 或文档支撑 |

### 场景选择建议

- 如果你面安全岗 → 选 A（安全事件响应）
- 如果你面后端/平台岗 → 选 C（数据排障）
- 如果你面基础架构岗 → 选 B（代码审查）

关键是：**场景越具体，Agent 的角色拆分、工具选型和安全设计就越有说服力。**

## 10. 进阶特性蓝

以下特性不要求全部实现，但在面试中被问到时能讲清楚设计思路即为加分。

### 多 Agent 辩论机制（设计即可）

- Verifier 不再是单 Agent，而是 2–3 个独立 Agent 对同一结论打分（事实准确性 + 逻辑完整性 + 来源充分性）
- 分数取平均，方差过大时触发人工判断
- 面试话术："LLM 单次判断有随机性，多个 Agent 独立打分取均值可以降低误判率"

### 沙箱工具执行（设计即可）

- 工具不在 Agent 进程内直接调用，而是提交到 Docker 容器中执行
- 每个工具调用创建临时容器，执行完销毁
- 网络隔离 + 文件系统只读 + 资源限制（CPU/内存/时间）
- 面试话术："工具执行可能产生副作用，沙箱隔离确保即便工具异常也不会影响主系统"

### A/B 评估框架（选做）

- 同一组测试问题，跑不同配置（不同 prompt / 不同检索策略 / 不同模型）→ 自动对比分数
- 用评分矩阵可视化差异
- 面试话术："系统的每个变更都有数据支撑，不是凭感觉调参数"

## 11. 执行建议

建议按 21 天版本推进，但每天都保证系统有一个可运行状态。

最重要的原则：

- 先让主链路跑通，再补企业级亮点。
- 先做可演示版本，再追求完整架构。
- 每个模块都要能解释设计理由。
- 不要一开始就做很复杂的权限、多租户和部署体系。
- 最终目标是让项目既能跑，也能在面试里讲得清楚。

## 12. 生产级升级路线

以下是在完成基础计划后，将项目从「可演示 Demo」提升为「看起来像真正生产系统」的增量内容。

### 12.1 测试体系（+3–4 天）

Demo 和生产系统之间最大的鸿沟。不需要 90% 覆盖率，但必须有策略性覆盖：

| 测试类型 | 内容 | 工具建议 |
|---|---|---|
| RAG 检索质量测试 | 10 条固定 query + 预期命中文档，每次改代码后跑一轮，确保检索不退化 | pytest |
| Agent 节点单元测试 | 每个 Agent 节点的输入/输出 schema 校验，Mock LLM 返回 | pytest + unittest.mock |
| 工具调用集成测试 | 模拟高风险工具被拦截 → 审批通过 → 继续执行的完整链路 | pytest + httpx |
| API 契约测试 | 关键接口返回 schema 不变，入参校验生效 | pytest + httpx |
| 一键回归脚本 | `make test` 一把跑完，README 里写清楚命令 | Makefile |

关键面试话术："每个 Agent 节点的输出 schema 有单元测试兜底，确保 LLM 返回格式异常时不会污染下游节点状态。"

### 12.2 错误处理与韧性（+2 天）

生产系统区别于 Demo 的核心特征：不是不出错，而是出错后不死。

| 添加内容 | 说明 |
|---|---|
| LLM 调用降级链 | 主模型超时 → 换 fallback 模型 → 返回优雅错误信息，不丢任务 |
| 向量库不可用降级 | Qdrant 挂了 → 跳过检索 → 告知用户「检索不可用时不直接回答业务结论，只返回降级提示，并允许用户稍后重试。」|
| 工具调用超时 + 熔断 | 工具超过 N 秒未响应 → 标记失败 → 不阻塞后续 Agent 节点 |
| 任务状态机 | pending → running → waiting_approval → completed / failed / cancelled，每个状态可恢复 |
| 部分成功处理 | 多步任务中某一步失败，前面结果不丢，用户可重试单步 |
| LLM 输出校验 | Agent 输出不符合 schema 的 JSON → 解析失败 → 重试或标记异常，记录原始输出 |

关键面试话术："Agent 系统的不确定性来自 LLM。我设计了输出校验层，解析失败会触发重试，两次失败后保留原始输出并标记异常，方便排查。"

### 12.3 可观测性升级（+2 天）

在当前 Trace 基础上向生产级靠拢：

| 添加内容 | 说明 |
|---|---|
| 结构化日志 | 用 `structlog`，每条日志带 trace_id、agent_name、task_id，JSON 格式输出 |
| 关键性能指标 | RAG 检索延迟 P50/P95/P99、LLM 首 token 时间、工具调用成功率、审批等待时长 |
| 告警规则（写出来即可） | "LLM 失败率 > 5% 持续 5 分钟 → 通知"；"审批队列积压 > 10 条 → 通知"，Dashboard 旁注明 |
| Langfuse 集成（可选） | 开源，一键接入查看调用链路，面试时直接打开看比截图更有说服力 |

### 12.4 部署与运维（+1–2 天）

Docker Compose 是开发环境，离生产差一步：

| 添加内容 | 说明 |
|---|---|
| 多环境配置 | `.env.dev` / `.env.prod`，通过 `ENV` 环境变量切换，不 hardcode |
| 数据库迁移（Alembic） | 管理 PostgreSQL schema 版本，`alembic upgrade head` 一条命令建表，不手动执行 SQL |
| 健康检查 | `/health` 端点检查 DB、Redis、LLM 服务连通性，Docker Compose 的 `healthcheck` 配置 |
| 运维脚本 | `make up` / `make down` / `make reset-db` / `make logs`，命令行体验统一 |
| 依赖安全扫描 | `pip-audit` 跑一次，结果放 CI 说明里，体现安全意识 |

### 12.5 API 设计与治理（+0.5 天）

| 添加内容 | 说明 |
|---|---|
| 统一错误响应格式 | 所有接口返回 `{"code": "...", "message": "...", "data": ...}` |
| 请求参数校验 | 每个 endpoint 入参用 Pydantic model 校验，FastAPI 自动生成 OpenAPI schema |
| API 文档 | `/docs` 页面信息完整，每个接口有中文 description |
| 分页 | 知识库列表、审批列表、日志列表统一分页，不一次性返回全量 |

### 12.6 安全升级（+1 天）

| 添加内容 | 说明 |
|---|---|
| API Key 鉴权 | 最简单的 JWT 或固定 API Key，不需要 OAuth，但 `/api/*` 接口不能裸奔 |
| 输入长度 / 文件大小限制 | 用户 query 最长 N 字符，文档上传最大 M MB，防止资源耗尽 |
| 依赖隔离设计 | 工具执行方案中写清楚隔离策略（subprocess / 沙箱），不一定要实现，但面试会被问 |
| CORS 白名单 | 生产环境只允许指定前端域名 |
| 密钥管理 | LLM API Key 放环境变量，`.env` 加入 `.gitignore`，提交 `.env.example` 不含真实值 |

### 12.7 架构文档（+1 天）

| 添加内容 | 说明 |
|---|---|
| 架构决策记录（ADR） | 3–5 个关键决策的简短记录（为什么选 LangGraph 而不是手写编排、为什么 Qdrant 而不是 Pinecone、为什么 FastAPI 而不是 Django）|
| 数据模型文档 | ER 图或至少一张表结构说明（user、knowledge_base、document、task、task_step、approval、tool_call_log 等）|
| 故障处理 Runbook | 5–8 条常见问题及排查步骤（「向量库连不上」「审批队列积压」「LLM 返回格式异常」）|

---

## 13. 升级优先级与调整后时间线

### 优先级

| 优先级 | 维度 | 理由 |
|---|---|---|
| P0 | 测试体系 + 错误处理 | 没有这两样，代码再好看都是 Demo |
| P1 | 部署运维（Alembic + 健康检查 + 多环境） | 成本低（1 天），观感提升巨大 |
| P2 | 结构化日志 + 告警规则 | 面试时 `docker logs` 全是 JSON，比 print 震撼得多 |
| P3 | API 治理 + 安全升级 + 架构文档 | 锦上添花，时间富余再做 |

### 调整后时间线

| 方案 | 内容 | 总耗时 |
|---|---|---|
| 最小方案 | 原计划（AI 辅助后）| 10–14 天 |
| 推荐方案 | 原计划 + P0 + P1 | 16–20 天 |
| 完整方案 | 原计划 + 全部 7 项 | 22–28 天 |

建议采用推荐方案（16–20 天）：测试 + 错误处理 + 运维基础，构建出一个面试官能感觉到「这个人做过正经工程」的质感，同时不过度拉长开发周期。
